apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupiter-chart.fullname" . }}
  labels:
    {{- include "jupiter-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jupiter-chart.selectorLabels" . | nindent 4 }}
